using System.Collections;
using UnityEngine;

public class SpecialSkill : MonoBehaviour {

    // Use this for initialization
    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }


    // Special Effect(sizeOfCircle, position, effectParticle)
    //吸收情绪和释放情绪
    //技能条积攒方式：击中敌人获得SP，没击中一次SP值加1。两个技能共用一个条，通过按键选择是用哪个技能。

    //吸收情绪技能效果：使情绪条充满至100，进入子弹时间。前三发子弹不与外界交换情绪。

    //释放情绪技能效果：使情绪释放至-100，进入额外高速移动。在固定时间内将情绪固定至-100，不与外界交换情绪。



}